name: manual get repo

on: 
  workflow_dispatch:

permissions:
  contents: read

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: get repo
        uses: victorvk2002/unit-tests@main
        with:
          tocken: ${{ secrets.PESONAL_TOKEN }}
