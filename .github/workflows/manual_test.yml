name: manual get repo

on: 
  workflow_dispatch:

permissions:
  contents: read

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: get repo
        uses: victorvk2002/unit-tests@v1
        with:
          username: victorvk2002
          password: ${{ secrets.PESONAL_TOKEN }}
